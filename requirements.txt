# ── LLM Providers ─────────────────────────────────────────────
google-genai>=1.0.0          # Gemini nuevo SDK (prioritario)
# google-generativeai>=0.7.0 # Gemini legacy SDK (fallback automático en client.py)
anthropic>=0.25.0
openai>=1.30.0               # También usado por Groq, DeepSeek, Mistral, OpenRouter

# ── Machine Learning ──────────────────────────────────────────
scikit-learn>=1.4.0
joblib>=1.3.0
numpy>=1.26

# ── Graph algorithms ──────────────────────────────────────────
networkx>=3.3

# ── Web framework ─────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
httpx>=0.27.0                # Requerido por FastAPI TestClient y algunos transports

# ── Visualization & CLI ───────────────────────────────────────
matplotlib>=3.9
rich>=13.7

# ── Config & utilities ────────────────────────────────────────
python-dotenv>=1.0
pydantic>=2.7
loguru>=0.7

# ── Testing ───────────────────────────────────────────────────
pytest>=8.2
pytest-cov>=5.0
pytest-asyncio>=0.23         # Para tests async (test_llm.py)

# ── Opcional: descarga del dataset Karrierewege_plus ──────────
# Instalar solo si vas a ejecutar backend/data/download_data.py:
# pip install datasets pandas
# datasets>=2.19
# pandas>=2.2.0
